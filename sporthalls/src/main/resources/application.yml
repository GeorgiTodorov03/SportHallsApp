spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sport_halls_db?useSSL=false&serverTimezone=Europe/Sofia&allowPublicKeyRetrieval=true
    username: root
    password: hE93VdGqb^dBdLC+
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080
